const ParticlesBackground = () => {
  return (
    <div className="fixed inset-0 -z-10">
      <div className="w-full h-full bg-gradient-animated bg-animated animate-gradient-flow" />
    </div>
  );
};

export default ParticlesBackground;